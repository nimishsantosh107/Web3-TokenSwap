import "./Swap.css";
import React from "react";

import SwapForm from "../../components/SwapForm/SwapForm";

const Swap = (props) => {
    return (
        <div>
            <SwapForm />
        </div>
    );
};

export default Swap;
